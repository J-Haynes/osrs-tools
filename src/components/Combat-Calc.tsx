import React from 'react'

import { Box, Typography } from '@mui/material'

export default function Combatlevel() {
  return (
    <Box display={'flex'} justifyContent={'center'}>
      <Typography>testtestets</Typography>
    </Box>
  )
}
